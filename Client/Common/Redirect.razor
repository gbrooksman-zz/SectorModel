@page "/Redirect/{modelId:guid}"

@inject NavigationManager nav

@code
{
	[Parameter]
	public Guid modelId { get; set; }

    protected override void OnInitialized()
    {
        nav.NavigateTo($"/Models/ModelItemList/{modelId}/true/true");
    }
}
